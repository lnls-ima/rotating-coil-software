################ HALL ##################
-> cycle durante ciclagem com cycling...

### testar:
-> ao mandar setpoint ele espera infinitamente, averiguar porque não da timeout

############### ROTATING COIL ###############

-> checar parametros antes de plotar curvas
-> Usar fontes caen ou versão nova do ELP?

-> melhorar config (configurar ao mandar plotar/ciclar?)
-> configura fonte ao clicar em send

##### Fontes #####
-> bloquear F1000
-> configurar PI p/ corretoras

- importar pydrs_api
- set_op_mode
#-. Lib.comm.drs.OpMode()
	arrumar valores no software 3-slowRef; 5-cycle.
#-. Lib.comm.drs.Read_ps_OpMode() 
	arrumar saida no software 1-cycle; 0-n�o cycle

-. drs.EnableSigGen()
-. drs.DisableSigGen()
-. drs.ConfigSigGen(sig_type, num_cycles, freq, amplitude, offset, aux0, aux1, aux2, aux3)
-. drs.Read_vDClink()

- drs.ResetInterlocks()
- Lib.comm.drs.Read_ps_SoftInterlocks()
- Lib.comm.drs.Read_ps_HardInterlocks()

- Lib.comm.drs.Connect()
- Lib.comm.drs.Disconnect()
- Lib.comm.drs.ser.is_open:
- Lib.comm.drs.SetSlaveAdd(i)
- Lib.comm.drs.TurnOn()
- Lib.comm.drs.TurnOff()
- Lib.comm.drs.ClosedLoop()
- Lib.comm.drs.SetISlowRef()

- Lib.comm.drs.Read_iLoad1()
- Lib.comm.drs.Read_ps_OnOff()
- Lib.comm.drs.Read_ps_OpenLoop()
#-. round(Lib.comm.drs.Read_vOutMod1()/2 + Lib.comm.drs.Read_vOutMod2()/2, 3)

#-. Lib.comm.drs.ConfigDPModule()
#-. Lib.comm.drs.Write_dp_ID(_id_mode)
#-. Lib.comm.drs.Write_dp_Class(_elp_PI_dawu)
#-. Lib.comm.drs.Write_dp_Coeffs(_list_coeffs.tolist())
	subst drs.set_dsp_coeffs(3, 0, [ki, kp, 0.95, -0.95]) bipolar
		  drs.set_dsp_coeffs(3, 0, [ki, kp, 0.95, 0]) monopolar

#-. Lib.comm.drs.Write_sigGen_Freq(float(_freq))
#-. Lib.comm.drs.Write_sigGen_Amplitude(float(_amp))
#-. Lib.comm.drs.Write_sigGen_Offset(float(_offset))
#-. Lib.comm.drs.Write_sigGen_Aux(float(
#-. Lib.comm.drs.ConfigSigGen()
	subst drs.cfg_siggen(sig_type, num_cycles, freq, amplitude, offset, aux0, aux1, aux2, aux3)
